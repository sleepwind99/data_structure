public class Assignment12 {

    public static void main(String[] args) {
       // Oracle o = new Oracle();
        int n, find, a, Lindex, Rindex, Mindex;

       // n = o.getN(); //bring in the number of lists from o
       // find = o.getWhatToFind(); //Get the data you want to find
        Lindex = 0; //Left index
        //Rindex = n-1; //Right index

     //   while(Lindex < Rindex) { //reduce the value of the index.
     //       Mindex = (Lindex+Rindex)/2;
         //   if(o.getElementAt(Mindex) < find) {
     //           Lindex = Mindex + 1;
            }
          //  else {
         //       Rindex = Mindex;
            }
      //  }
      //  if((Lindex == Rindex) && (o.getElementAt(Lindex)==find)) {
       //     o.reportAnswer(true, Lindex); //Return index of found value to o
     //   }
       // else {
        //    o.reportAnswer(false, 0); //If not found
       // }


    //}

//}
